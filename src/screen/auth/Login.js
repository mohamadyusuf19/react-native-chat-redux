import React, { Component } from 'react';
import {  View, Text, } from 'react-native';
import LoginForm from '../../config/Navigator';

export default class Login extends Component {
  render() {
    return (
      <LoginForm/>
    );
  }
}
